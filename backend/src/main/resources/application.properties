spring.application.name=money-manager
server.port=${PORT:8080}

# Supabase PostgreSQL Database (uses env vars for production)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:postgres.mlpleqpoaxdexeprceqm}
spring.datasource.password=${DATABASE_PASSWORD:Shivapoojitha}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=false

# Remove H2 Console (not needed for Supabase)
spring.h2.console.enabled=false

# JWT
jwt.secret=${JWT_SECRET:MySecretKeyForJWTTokenGenerationThatIsLongEnough12345678901234567890}
jwt.expiration=86400000

# CORS (allow all origins for mobile app)
cors.allowed-origins=*
