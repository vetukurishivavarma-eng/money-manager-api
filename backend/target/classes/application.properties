spring.application.name=money-manager
server.port=8080

# Supabase PostgreSQL Database
spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.mlpleqpoaxdexeprceqm
spring.datasource.password=Shivapoojitha
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true

# Remove H2 Console (not needed for Supabase)
spring.h2.console.enabled=false

# JWT
jwt.secret=MySecretKeyForJWTTokenGenerationThatIsLongEnough12345678901234567890
jwt.expiration=86400000

# CORS
cors.allowed-origins=http://localhost:4200